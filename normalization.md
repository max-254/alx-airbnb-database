# normalization

Instead of having one massive PROPERTY table with 30+ columns, we split it into 5 related tables:
PROPERTY (core listing info)
LOCATION (geographic data)
PROPERTY -IMAGE (photos)
AMENITY (features)
